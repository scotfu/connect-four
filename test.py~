def show_board(board):
    board=deepcopy(board)
    #refill
    for y in range(0,HEIGHT):
        for x in range(0,WIDTH):
            try:
                print x,y
                board[x][y]
            except:
                board[x].append(None)
